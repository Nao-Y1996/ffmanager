
<div class="container">
	 <div class='row'>
		<div class='col-xs-2'></div>
		<div class="col-xs-8 genre-container">
			<% @genres.each do |genre| %>
				  <%=link_to genre.name, genre_path(genre) ,class: "genre-box",:style=>"color:white;"  %>

			<%end%>
		</div>
		<div class='col-xs-2'></div>
	</div>
</div>